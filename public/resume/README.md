# Resume PDF

Place your résumé here as:

```
Darren_Tang_Resume.pdf
```

The Resume page and Contact page reference this exact filename. If you rename the
file, update `RESUME_PDF_PATH` in `src/data/socials.ts`.

Until a real PDF is added, the embedded viewer will fall back to a download prompt.
